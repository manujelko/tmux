# My tmux config


## Install

1. Clone repo in `$HOME/.config/tmux`.
2. Add a function / alias to `.zshrc` or `.bashrc` or equivalent.

```zsh
# tmux
function sesh {
        $HOME/.config/tmux/se.sh
}
```
